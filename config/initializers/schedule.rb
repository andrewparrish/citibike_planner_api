# DEPRECATED -- moved to resque scheduler
# require Rails.root.join('lib', 'scheduler')

# scheduler = Scheduler.get_scheduler
# if Rails.env == "production"
#   Scheduler.add_event('1m', UpdateStations)
# end
